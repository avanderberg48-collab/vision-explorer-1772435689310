#!/usr/bin/env sh

# Gradle wrapper script placeholder
# This will be handled by gradle/gradle-build-action in GitHub Actions

echo "This gradlew wrapper is designed to work with GitHub Actions"
exit 0